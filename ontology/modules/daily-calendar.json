{
  "profile_version": "0.3.0",
  "schema": "schema/daily-calendar.schema.json",
  "class": "SearchDay",
  "fields": {
    "reviewId": {
      "semantic_uri": "cile:reviewId",
      "definition": "reviewId in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "timezone": {
      "semantic_uri": "cile:timezone",
      "definition": "timezone in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "scheduledLocalTime": {
      "semantic_uri": "cile:scheduledLocalTime",
      "definition": "scheduledLocalTime in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "asOf": {
      "semantic_uri": "cile:asOf",
      "definition": "asOf in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "coverageStart": {
      "semantic_uri": "cile:coverageStart",
      "definition": "coverageStart in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "lastLedgerDate": {
      "semantic_uri": "cile:lastLedgerDate",
      "definition": "lastLedgerDate in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "rows": {
      "semantic_uri": "cile:rows",
      "definition": "rows in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "expectedDays": {
      "semantic_uri": "cile:expectedDays",
      "definition": "expectedDays in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "completedDays": {
      "semantic_uri": "cile:completedDays",
      "definition": "completedDays in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "missingDays": {
      "semantic_uri": "cile:missingDays",
      "definition": "missingDays in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "completionRate": {
      "semantic_uri": "cile:completionRate",
      "definition": "completionRate in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "sourceCompletionRate30": {
      "semantic_uri": "cile:sourceCompletionRate30",
      "definition": "sourceCompletionRate30 in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "date": {
      "semantic_uri": "cile:date",
      "definition": "date in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "status": {
      "semantic_uri": "cile:status",
      "definition": "status in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "ledgerPresent": {
      "semantic_uri": "cile:ledgerPresent",
      "definition": "ledgerPresent in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "startedAt": {
      "semantic_uri": "cile:startedAt",
      "definition": "startedAt in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "finishedAt": {
      "semantic_uri": "cile:finishedAt",
      "definition": "finishedAt in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "attemptCount": {
      "semantic_uri": "cile:attemptCount",
      "definition": "attemptCount in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "queriesPlanned": {
      "semantic_uri": "cile:queriesPlanned",
      "definition": "queriesPlanned in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "queriesCompleted": {
      "semantic_uri": "cile:queriesCompleted",
      "definition": "queriesCompleted in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "completedSources": {
      "semantic_uri": "cile:completedSources",
      "definition": "completedSources in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "expectedSources": {
      "semantic_uri": "cile:expectedSources",
      "definition": "expectedSources in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    },
    "failureCodes": {
      "semantic_uri": "cile:failureCodes",
      "definition": "failureCodes in the explicitly scoped Rome calendar projection; absent execution is never a measured zero."
    }
  },
  "scopes": {
    "legacy": {
      "coverage_start": "2026-08-31",
      "expected_sources": [
        "Consensus",
        "Exa"
      ]
    },
    "oa-2026-09-08": {
      "coverage_start": "2026-09-09",
      "policy": "OA-1",
      "reset_at": "2026-09-08T08:29:28Z",
      "expected_sources": [
        "Exa"
      ]
    }
  },
  "surveillance_contract": {
    "schema": "schema/surveillance-run.schema.json",
    "schema_version": 3,
    "legacy_schema": "schema/surveillance-run-v1.schema.json",
    "source_class": "SearchActivity",
    "source_slot": "source_name",
    "protocol_version": "CILE-DAILY-v4",
    "authority": "Owner instruction 2026-09-08: Exa-only automatic provisional registration before individual analysis; retain v1/v2 history.",
    "retained_v2_schema": "schema/surveillance-run-v2.schema.json"
  },
  "extraordinary_executions": {
    "class": "SearchActivity",
    "identity_slot": "event_id",
    "batch_pattern": "ACADEMIC-YYYY-MM-DD-EXTRA-<12 lowercase hexadecimal characters>",
    "public_container": "extraRuns",
    "public_fields": {
      "batchId": {
        "semantic_uri": "cile:eventId"
      },
      "date": {
        "semantic_uri": "cile:date"
      },
      "startedAt": {
        "semantic_uri": "cile:startedAt"
      },
      "finishedAt": {
        "semantic_uri": "cile:finishedAt"
      },
      "status": {
        "semantic_uri": "cile:status"
      },
      "queriesPlanned": {
        "semantic_uri": "cile:queriesPlanned"
      },
      "queriesCompleted": {
        "semantic_uri": "cile:queriesCompleted"
      },
      "occurrencesReturned": {
        "semantic_uri": "cile:occurrencesReturned"
      },
      "uniqueResults": {
        "semantic_uri": "cile:uniqueResults"
      },
      "intakeCandidates": {
        "semantic_uri": "cile:intakeCandidates"
      },
      "failureCode": {
        "semantic_uri": "cile:failureCodes"
      }
    },
    "semantics": "Aggregate SearchActivity telemetry; distinct execution IDs on the same Rome day. Excluded from SearchDay scheduled completeness and scientific decisions. Existing operational quantities only.",
    "authority": "Owner request to resolve same-day extraordinary-run and OA acquisition blockers, 2026-09-08."
  }
}
