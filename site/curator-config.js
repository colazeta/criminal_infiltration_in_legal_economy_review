"use strict";

// This URL is intentionally empty until the reviewed GitHub App backend has
// been deployed and its exact HTTPS origin has been verified.
window.CURATOR_APP_CONFIG = Object.freeze({
  apiBaseUrl: "",
});
