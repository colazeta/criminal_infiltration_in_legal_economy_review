# Review protocol entry point

The authoritative, version-controlled protocol is
[`docs/literature_review_protocol.md`](docs/literature_review_protocol.md).

This file is retained as a stable entry point for older links. It no longer
contains a second protocol because the previous duplicate used different
eligibility terms and a conflicting saturation threshold.
