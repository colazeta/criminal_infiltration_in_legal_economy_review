from pathlib import Path
import json
r=Path('.')
p=r/'ontology/modules/completion-policy.json';d=json.loads(p.read_text());d['group_assessment_fields']={g:f'{g}_assessment' for g in d['mandatory'] if g!='root'};d['group_missingness_outcomes']=['not_reported','not_applicable'];d['group_assessment_semantics']='Every empty governed group requires its own exact-head human missingness assessment: not_reported or not_applicable after full-text examination. Engineering packets leave these null; true, recorded, missing or uncertain are invalid for an empty group. Non-empty groups are recorded. The version-bound checklist digest is retained in the immutable receipt.';p.write_text(json.dumps(d,indent=2)+'\n')
p=r/'curator-app/src/completion-policy.js';s=p.read_text();s+='''\nexport function groupReviewTemplate(input) {\n  return Object.fromEntries(Object.entries(policy.group_assessment_fields).map(([group,key])=>{\n    if(!Array.isArray(input[group]))throw Error('completion_group_missing');\n    return [key,input[group].length?'recorded':null];\n  }));\n}\nexport function validateGroupReview(template,checklist) {\n  for(const [key,value]of Object.entries(template)) {\n    const accepted=value==='recorded'?checklist[key]==='recorded':policy.group_missingness_outcomes.includes(checklist[key]);\n    if(!accepted)throw Error('empty_group_assessment_required:'+key);\n  }\n}\n// A finite set (at most 32) permits validation of the retained checklist hash\n// without exposing or re-fetching the private human decision manifest.\nexport function possibleGroupReviews(input) {\n  let choices=[{}];\n  for(const [key,value]of Object.entries(groupReviewTemplate(input))) {\n    const values=value==='recorded'?['recorded']:policy.group_missingness_outcomes;\n    choices=choices.flatMap(row=>values.map(item=>({...row,[key]:item})));\n  }\n  return choices;\n}\n''';p.write_text(s)
p=r/'curator-app/src/enrichment-adjudication.js';s=p.read_text();s=s.replace('COMPLETION_POLICY,inspectCompletionFacts,validateFrameworkAssessment}', 'COMPLETION_POLICY,inspectCompletionFacts,validateFrameworkAssessment,groupReviewTemplate,validateGroupReview,possibleGroupReviews}')
s=s.replace('checklist:Object.fromEntries(CHECKS.map(k=>[k,null]))','checklist:{...Object.fromEntries(CHECKS.map(k=>[k,null])),...groupReviewTemplate(state.input)}')
s=s.replace("exact(manifest,keys,'completion_manifest');exact(manifest.checklist,CHECKS,'completion_checklist');\n  const expected={...packet.manifest,checklist:Object.fromEntries(CHECKS.map(k=>[k,true]))};", "exact(manifest,keys,'completion_manifest');exact(manifest.checklist,Object.keys(packet.manifest.checklist),'completion_checklist');\n  const groups=Object.fromEntries(Object.entries(packet.manifest.checklist).filter(([key])=>!CHECKS.includes(key)));\n  validateGroupReview(groups,manifest.checklist);\n  const expected={...packet.manifest,checklist:{...Object.fromEntries(CHECKS.map(k=>[k,true])),...Object.fromEntries(Object.keys(groups).map(key=>[key,manifest.checklist[key]]))}};")
s=s.replace("sha256(canonicalJson(manifest.checklist)),manifestHash", "sha256(canonicalJson({policy:COMPLETION_POLICY,checklist:manifest.checklist})),manifestHash")
s=s.replace("if(state.proposal.payload_sha256!==receipt.proposal_sha256)throw Error('stale_receipt');", "if(state.proposal.payload_sha256!==receipt.proposal_sha256)throw Error('stale_receipt');\n    let checklistValid=false;\n    for(const groups of possibleGroupReviews(state.input)){\n      const checklist={...Object.fromEntries(CHECKS.map(k=>[k,true])),...groups};\n      if(await sha256(canonicalJson({policy:COMPLETION_POLICY,checklist}))===receipt.checklist_sha256){checklistValid=true;break}\n    }\n    if(!checklistValid)throw Error('completion_policy_receipt_stale');")
p.write_text(s)
p=r/'curator-app/test/enrichment-adjudication.test.js';s=p.read_text();s=s.replace("const encoded=value=>", "const reviewedChecklist=packet=>Object.fromEntries(Object.entries(packet.manifest.checklist).map(([k,v])=>[k,k.endsWith('_assessment')?(v==='recorded'?'recorded':'not_applicable'):true]));\nconst encoded=value=>")
s=s.replace('Object.fromEntries(Object.keys(packet.manifest.checklist).map(k=>[k,true]))','reviewedChecklist(packet)')
s+='''\ntest('empty groups require explicit individual human missingness decisions, not generic checklist booleans',async()=>{\n  const x=await prepared();await importCalibrationApproval(x.env,{calibration_id:'CAL-2026-001',pr_number:10},approvedGet(calibrationManifest),now);\n  const packet=await completionPacket(x.env,x.target.target_id);\n  const keys=['studies','datasets','analyses','variable_uses','findings'].map(k=>k+'_assessment');\n  for(const key of keys)assert.equal(packet.manifest.checklist[key],null);\n  for(const key of keys)for(const bad of [null,true,'recorded','not_verifiable','ambiguous']) {\n    const manifest={...packet.manifest,checklist:{...reviewedChecklist(packet),[key]:bad}};\n    await assert.rejects(importCompletionApproval(x.env,{target_id:x.target.target_id,pr_number:11},approvedGet(manifest,11),now+1000),/empty_group_assessment_required/);\n  }\n  assert.equal(x.sqlite.prepare('SELECT COUNT(*) n FROM enrichment_adjudication_receipts').get().n,0);\n  const manifest={...packet.manifest,checklist:reviewedChecklist(packet)};\n  manifest.checklist.datasets_assessment='not_reported';\n  await importCompletionApproval(x.env,{target_id:x.target.target_id,pr_number:11},approvedGet(manifest,11),now+1000);\n  assert.equal((await publicCompletionState(x.env,record.id)).completed,true);\n});\n''';p.write_text(s)
p=r/'scripts/query_checkpoint.py';s=p.read_text().replace('from urllib.parse import urlsplit','from urllib.parse import urlsplit, parse_qsl, unquote')
s=s.replace("for k in [v.split('=')[0] for v in u.query.split('&')]", "for k in [unquote(key) for key,_ in parse_qsl(u.query,keep_blank_values=True)]")
p.write_text(s)
p=r/'tests/test_query_checkpoint.py';s=p.read_text();s+='''\n    def test_percent_encoded_credential_keys_never_enter_public_checkpoint(self):\n        for query in ['%74oken=opaque','X-Amz-%53ignature=opaque','%2574oken=opaque','signature=opaque','api%5fkey=opaque','x-goog-signature=opaque']:\n            d=self.fixture();d['occurrences'][0]['url']='https://example.org/paper?'+query\n            with self.assertRaisesRegex(ValueError,'checkpoint_private_locator'):validate(d)\n        d=self.fixture();d['occurrences'][0]['url']='https://example.org/paper?paper%5fid=123';validate(d)\n''';p.write_text(s)
p=r/'.github/workflows/deploy-curator-worker.yml';s=p.read_text().replace('      - "config/archive-cycle.json"','      - "config/archive-cycle.json"\n      - "config/verified-document-seed.json"');p.write_text(s)
p=r/'tests/test_delivery_contracts.py';s=p.read_text();s+='''\n    def test_seed_only_corrections_trigger_existing_deployment(self):\n        workflow=(ROOT/'.github/workflows/deploy-curator-worker.yml').read_text()\n        self.assertIn('      - "config/verified-document-seed.json"',workflow.split('permissions:')[0])\n''';p.write_text(s)
p=r/'docs/operations/two-lane-delivery.md';s=p.read_text();s=s.replace('Empty arrays never prove that a paper has no studies, variables or findings.', 'Empty arrays never prove that a paper has no studies, variables or findings. Each empty group has its own null assessment in the review checklist. Only an exact-head human decision of `not_reported` or `not_applicable` after full-text examination can close it; a generic true checklist cannot. Non-empty groups are marked `recorded`. The receipt binds the policy and complete group-level checklist digest.');p.write_text(s)
